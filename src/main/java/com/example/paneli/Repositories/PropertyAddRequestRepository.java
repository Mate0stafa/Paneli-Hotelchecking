package com.example.paneli.Repositories;

import com.example.paneli.Models.Auth.PropertyAddRequest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PropertyAddRequestRepository extends JpaRepository<PropertyAddRequest, Long> {
}